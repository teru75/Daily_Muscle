<div class="container">
  <div class="row my-menu-content">
    <h1 class="page-header">マイメニュー　一覧</h1>
    <div class="col-xs-7">
      <div>
        <div style="margin-bottom: 20px;">
          <% if @customer == current_customer %>
          <span><%= link_to "新規登録する",new_customer_my_menu_path, class:" btn btn-primary" %></span>
          <% end %>
        </div>
      </div>
      <% if @whole_bodys.exists? %>
      <div>
        <h4>全身</h4>
        <div class="my-menu-item">
          <%= render "my_menus/menu_index_item", whole_bodys: @whole_bodys %>
        </div>
      </div>
      <% end %>

      <% if @upper_bodys.exists? %>
      <div>
        <h4>上半身</h4>
        <div class="my-menu-item">
          <%= render "my_menus/menu_index_item", whole_bodys: @upper_bodys %>
        </div>
      </div>
      <% end %>

      <% if @breasts.exists? %>
      <div>
        <h4>胸</h4>
        <div class="my-menu-item">
          <%= render "my_menus/menu_index_item", whole_bodys: @breasts %>
        </div>
      </div>
      <% end %>

      <% if @arms.exists? %>
      <div>
        <h4>腕</h4>
        <div class="my-menu-item">
          <%= render "my_menus/menu_index_item", whole_bodys: @arms %>
        </div>
      </div>
      <% end %>

      <% if @shoulders.exists? %>
      <div>
        <h4>肩</h4>
        <div class="my-menu-item">
          <%= render "my_menus/menu_index_item", whole_bodys: @shoulders %>
        </div>
      </div>
      <% end %>

      <% if @backs.exists? %>
      <div>
        <h4>背中</h4>
        <div class="my-menu-item">
          <%= render "my_menus/menu_index_item", whole_bodys: @backs %>
        </div>
      </div>
      <% end %>

      <% if @lower_bodys.exists? %>
      <div>
        <h4>下半身</h4>
        <div class="my-menu-item">
          <%= render "my_menus/menu_index_item", whole_bodys: @lower_bodys %>
        </div>
      </div>
      <% end %>

      <% if @trunks.exists? %>
      <div>
        <h4>体幹</h4>
        <div class="my-menu-item">
          <%= render "my_menus/menu_index_item", whole_bodys: @trunks %>
        </div>
      </div>

      <% end %>
    </div>
  <%= render "customers/customer_form", customer: @customer %>
  </div>
</div>