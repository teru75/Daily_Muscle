<div class="container">
  <div class="row my-menu-content">
    <div class="col-xs-8">
      <div>
        <h3>マイメニュー　一覧</h3>
        <div style="margin-bottom: 20px;">
        <% if @customer == current_customer %>
        <span><%= link_to "新規登録する",new_customer_my_menu_path, class:" btn btn-primary" %></span>
        <% end %>
        </div>
      </div>

      <% if @whole_bodys.exists? %>
      <div>
        <h4>全身</h4>
        <div class="my-menu-item">
          <% @whole_bodys.each do |menu| %>
          <div style="margin-right: 20px; width: 200px;">
            <table class="table">
              <tr>
                <th>
                  <%= link_to customer_my_menu_path(menu.customer_id, menu) do %>
                  <%= menu.theme %>
                  <% end %>
                </th>
              </tr>
              <% menu.my_menu_items.each do |item| %>
              <tr>
                <td>
                <%= item.name %>
                </td>
              </tr>
              <% end %>
            </table>
          </div>
          <% end %>
        </div>
      </div>
      <% end %>

      <% if @upper_bodys.exists? %>
      <div>
        <h4>上半身</h4>
        <div class="my-menu-item">
          <% @upper_bodys.each do |menu| %>
          <div style="margin-right: 20px; width: 200px;">
            <table class="table">
              <tr>
                <th>
                  <%= link_to customer_my_menu_path(menu.customer_id, menu) do %>
                  <%= menu.theme %>
                  <% end %>
                </th>
              </tr>
              <% menu.my_menu_items.each do |item| %>
              <tr>
                <td>
                <%= item.name %>
                </td>
              </tr>
              <% end %>
            </table>
          </div>
          <% end %>
        </div>
      </div>
      <% end %>

      <% if @breasts.exists? %>
      <div>
        <h4>胸</h4>
        <div class="my-menu-item">
          <% @breasts.each do |menu| %>
          <div style="margin-right: 20px; width: 200px;">
            <table class="table">
              <tr>
                <th>
                  <%= link_to customer_my_menu_path(menu.customer_id, menu) do %>
                  <%= menu.theme %>
                  <% end %>
                </th>
              </tr>
              <% menu.my_menu_items.each do |item| %>
              <tr>
                <td>
                <%= item.name %>
                </td>
              </tr>
              <% end %>
            </table>
          </div>
          <% end %>
        </div>
      </div>
      <% end %>

      <% if @arms.exists? %>
      <div>
        <h4>腕</h4>
        <div class="my-menu-item">
          <% @arms.each do |menu| %>
          <div style="margin-right: 20px; width: 200px;">
            <table class="table">
              <tr>
                <th>
                  <%= link_to customer_my_menu_path(menu.customer_id, menu) do %>
                  <%= menu.theme %>
                  <% end %>
                </th>
              </tr>
              <% menu.my_menu_items.each do |item| %>
              <tr>
                <td>
                <%= item.name %>
                </td>
              </tr>
              <% end %>
            </table>
          </div>
          <% end %>
        </div>
      </div>
      <% end %>

      <% if @shoulders.exists? %>
      <div>
        <h4>肩</h4>
        <div class="my-menu-item">
          <% @shoulders.each do |menu| %>
          <div style="margin-right: 20px; width: 200px;">
            <table class="table">
              <tr>
                <th>
                  <%= link_to customer_my_menu_path(menu.customer_id, menu) do %>
                  <%= menu.theme %>
                  <% end %>
                </th>
              </tr>
              <% menu.my_menu_items.each do |item| %>
              <tr>
                <td>
                <%= item.name %>
                </td>
              </tr>
              <% end %>
            </table>
          </div>
          <% end %>
        </div>
      </div>
      <% end %>

      <% if @backs.exists? %>
      <div>
        <h4>背中</h4>
        <div class="my-menu-item">
          <% @backs.each do |menu| %>
          <div style="margin-right: 20px; width: 200px;">
            <table class="table">
              <tr>
                <th>
                  <%= link_to customer_my_menu_path(menu.customer_id, menu) do %>
                  <%= menu.theme %>
                  <% end %>
                </th>
              </tr>
              <% menu.my_menu_items.each do |item| %>
              <tr>
                <td>
                <%= item.name %>
                </td>
              </tr>
              <% end %>
            </table>
          </div>
          <% end %>
        </div>
      </div>
      <% end %>

      <% if @lower_bodys.exists? %>
      <div>
        <h4>下半身</h4>
        <div class="my-menu-item">
          <% @lower_bodys.each do |menu| %>
          <div style="margin-right: 20px; width: 200px;">
            <table class="table">
              <tr>
                <th>
                  <%= link_to customer_my_menu_path(menu.customer_id, menu) do %>
                  <%= menu.theme %>
                  <% end %>
                </th>
              </tr>
              <% menu.my_menu_items.each do |item| %>
              <tr>
                <td>
                <%= item.name %>
                </td>
              </tr>
              <% end %>
            </table>
          </div>
          <% end %>
        </div>
      </div>
      <% end %>

      <% if @trunks.exists? %>
      <div>
        <h4>体幹</h4>
        <div class="my-menu-item">
          <% @trunks.each do |menu| %>
          <div style="margin-right: 20px; width: 200px;">
            <table class="table">
              <tr>
                <th>
                  <%= link_to customer_my_menu_path(menu.customer_id, menu) do %>
                  <%= menu.theme %>
                  <% end %>
                </th>
              </tr>
              <% menu.my_menu_items.each do |item| %>
              <tr>
                <td>
                <%= item.name %>
                </td>
              </tr>
              <% end %>
            </table>
          </div>
          <% end %>
        </div>
      </div>
      <% end %>
    </div>

    <div class="col-xs-4" style="border: 2px solid #eee; background-color: #eeeaec;">
      <div class="customer-profile" style="width:50%; margin: 70px auto; ">
      <div class="profile-image" style="width: 40%; margin: 20px auto;">
      <%= render "customers/customer_form", customer: @customer %>
    </div>
  </div>
</div>