<div class="row">
  <% if event.errors.any? %>
    <div class="error_explanation">
      <h2><%= event.errors.count %>件のエラーがありました。</h2>
      <ul>
        <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
</div>